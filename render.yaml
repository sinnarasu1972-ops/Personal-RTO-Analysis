services:
  - type: web
    name: mahindra-rto-dashboard
    runtime: python
    pythonVersion: 3.12.10
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
